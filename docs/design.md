# 디자인 스펙 — 머나먼약속 (A Distant Promise)

> 비주얼 디자인, 맵 구성, 캐릭터 스프라이트, 애니메이션 효과 전체 규격서.
> 개발 시 이 문서를 기준으로 구현한다.
> Phase 0 MVP는 **Placeholder(색상 블록)** 로 동작 검증 후 실제 에셋으로 교체한다.

---

## 1. 비주얼 컨셉 & 컬러 팔레트

### 전체 톤
- **군사 전략 게임**: 어두운 배경 + 선명한 유닛 색상 대비
- **모바일 최적화**: 굵은 외곽선, 큰 아이콘, 한눈에 구분 가능한 색상 코딩

### 컬러 팔레트

```
배경/지형
  풀밭:       #4a7c59  (짙은 초록)
  도로:       #8b7355  (흙갈색)
  산/장애물:  #5a5a6b  (회청색)
  하늘:       #1a1a2e  (네이비, 전체 배경)

유닛 (Placeholder → 실제 스프라이트 교체 시 유지)
  보병:       #4488ff  (파랑)
  전차:       #ffaa00  (주황)
  공군:       #44ffcc  (청록)
  특수:       #ff44aa  (핑크)
  적군 유닛:  위 색상에 붉은 외곽선 (#ff2222, 3px)

거점
  아군:       #2255dd  (진파랑)
  적군:       #dd2222  (진빨강)
  중립:       #888888  (회색)
  점령 진행:  #ffdd00  (황금, 프로그레스 링)

UI
  HUD 배경:   rgba(0, 0, 0, 0.6)
  버튼 기본:  #334466
  버튼 활성:  #4488ff
  버튼 비활:  #222233
  승리:       #44ff88
  패배:       #ff4444
  텍스트:     #ffffff / #cccccc (보조)
```

### 폰트
- **게임 내**: `"Press Start 2P"` (Google Fonts, 픽셀 느낌) — 숫자/영문
- **한글**: `"Noto Sans KR"` — UI 텍스트
- **크기 기준**: HUD 수치 18px / 버튼 레이블 14px / 유닛 이름 10px

---

## 2. 맵 구성 (800×600 BattleScene)

### 2-1. 타일 레이어 구조

```
Layer 3 (top)   : 유닛, 투사체, 파티클
Layer 2         : 거점 오브젝트 (원형 + 프로그레스 링)
Layer 1         : 지형 오브젝트 (산, 나무 등 장식)
Layer 0 (base)  : 바닥 타일 (풀/도로)
```

### 2-2. 타일 사이즈 & 그리드

```
타일 1개: 40×40 px
맵 전체: 800×600 = 20×15 타일
```

### 2-3. 지형 구역 배치 (좌표 기준)

```
┌────────────────────────────────────────────┐ y=0
│##########  산 장애물  ##########           │ y=0~80
│   [적 거점 ★]  (x=150, y=120)             │
│   풀(0,80)────────────────────────풀       │
│         ╲                                  │
│    풀    ╲  대각 도로                      │ y=200
│           ╲    [중립 거점 ★]              │
│            ╲   (x=400, y=300)              │ y=300
│             ╲                              │
│              ╲    풀                       │
│   풀          ╲                            │ y=400
│                ╲   [아군 거점 ★]          │
│  ##########     ╲   (x=650, y=500)         │
│   산 장애물  풀  ╲━━━━━━━━━━━━━━━━         │ y=520~600
└────────────────────────────────────────────┘ y=600
  x=0                                   x=800

범례: ★=거점, ##=산(장애물), ╲=대각 도로
```

### 2-4. 도로 (이동 경로)

- **대각 도로**: (x=50,y=50) → (x=750,y=550) 방향 대각선
- 너비: 80px
- 색상: `#8b7355` + `#9a8866` (가장자리 밝게)
- 도로 위 유닛 이동속도 +20% 보너스 (Phase 1+)

### 2-5. 산/장애물

- 좌상단 블록: x=180~320, y=0~140
- 우하단 블록: x=480~620, y=460~600
- 색상: `#5a5a6b` 기본 + `#3a3a4b` 음영
- 유닛 통과 불가 (Arcade Physics: `setImmovable(true)`)
- 그림자 효과: 하단에 `#00000033` 4px 그림자

### 2-6. 거점 (CapturePoint) 비주얼

```
구성 요소:
1. 기단 원:       반경 35px, 해당 owner 색상, alpha 0.85
2. 외곽 링:       반경 38px, 흰색 stroke 2px
3. 깃발 아이콘:   중앙 16×16px (아군=🏴, 적군=⚑, 중립=○)
4. 점령 프로그레스 링:
   - 반경 42px
   - 황금색(#ffdd00) arc, 두께 4px
   - 0~360도 = 0~100% 점령 진행률
   - 시계방향으로 채워짐
5. HP 바:
   - 거점 아래 y+45 위치
   - 70×6px, 초록→빨강 그라디언트
```

---

## 3. 유닛 스프라이트 규격

### 3-1. Placeholder (Phase 0)

실제 아트 없이 색상 블록으로 구현:

```
유닛 바디:   28×28px 둥근 사각형 (radius 4px)
외곽선:      2px solid 흰색 (아군) / 2px solid #ff4444 (적군)
티어 배지:   우상단 10×10px 원, 흰 숫자
HP 바:       유닛 상단 y-20, 28×4px
이름표:      유닛 하단 y+18, 8px 폰트
```

### 3-2. 실제 스프라이트 시트 규격 (Phase 1 교체용)

```
캔버스 1장: 512×256 px (스프라이트 시트)
프레임 크기: 64×64 px
배열: 8열 × 4행

행 구성:
  Row 0 (y=0):   idle 애니메이션     — 4프레임 (64×64 × 4)
  Row 1 (y=64):  move 애니메이션     — 6프레임 (64×64 × 6)
  Row 2 (y=128): attack 애니메이션   — 4프레임 (64×64 × 4)
  Row 3 (y=192): die 애니메이션      — 4프레임 (64×64 × 4)

파일명 규칙:
  public/assets/sprites/
    infantry.png   (보병)
    tank.png       (전차)
    air.png        (공군)
    special.png    (특수)
    enemy_infantry.png  (적 보병, 붉은 팔레트 교체본)
    ...
```

---

## 4. 유닛 애니메이션

### 4-1. Idle (대기)

| 유닛 | 프레임 수 | 속도 | 동작 |
|---|---|---|---|
| 보병 | 4 | 8 fps | 숨쉬기 (상하 2px 흔들림) |
| 전차 | 2 | 4 fps | 포탑 미세 회전 |
| 공군 | 4 | 10 fps | 호버링 (상하 3px + 좌우 1px) |
| 특수 | 4 | 6 fps | 망토 흔들림 |

**Placeholder 구현 (Phase 0)**:
```typescript
// Phaser Tween으로 구현 — new 객체 없이 재사용
this.scene.tweens.add({
  targets: this.sprite,
  y: '-=3',
  duration: 800,
  yoyo: true,
  repeat: -1,
  ease: 'Sine.easeInOut',
});
```

### 4-2. Move (이동)

| 유닛 | 동작 | 구현 방법 |
|---|---|---|
| 보병 | 달리기 — 다리 움직임, 좌우 약간 흔들 | 스프라이트 애니메이션 + Tween |
| 전차 | 직선 이동 — 캐터필러 회전 | 스프라이트 애니메이션 |
| 공군 | 기울며 이동 — 이동 방향으로 10도 기울기 | rotation Tween |
| 특수 | 보병과 유사, 더 빠른 발걸음 | 스프라이트 애니메이션 |

**이동 방향 회전 (Placeholder Phase 0)**:
```typescript
// 목표 방향으로 부드럽게 회전
const angle = Phaser.Math.Angle.Between(this.x, this.y, tx, ty);
this.scene.tweens.add({
  targets: this,
  rotation: angle,
  duration: 150,
  ease: 'Linear',
});
```

**이동 먼지 파티클 (도로 위)**:
```typescript
// ObjectPool의 작은 원 3개로 구현 (new 금지)
// 뒤쪽에 fadeOut 흔적 — alpha 0.4→0, 지름 4px, 0.3초
```

### 4-3. Attack (공격)

#### 근접 공격 (보병, 전차)

```
1. 공격 시작: 목표 방향으로 0.1초 내 15px 전진 Tween
2. 타격 순간: 흰색 플래시 0.05초 (sprite.setTintFill(0xffffff))
3. 복귀:      원위치로 0.15초 복귀 Tween
4. 피격 유닛: 빨간 플래시 0.1초 (sprite.setTint(0xff4444))

총 지속: 0.3초
```

```typescript
// 공격 애니메이션 Tween 체인
this.scene.tweens.chain({
  targets: this,
  tweens: [
    { x: '+=15', duration: 100, ease: 'Expo.Out' },
    { x: '-=15', duration: 150, ease: 'Bounce.Out' },
  ],
});
```

#### 원거리 공격 (공군, 특수)

```
투사체 비주얼:
  공군:  노란 원 (radius 5px) + 뒤에 꼬리 (4개 연속 alpha 감소 원)
  특수:  보라색 마름모 (8×8px)

발사 이펙트: 총구 위치에 0.1초 흰 플래시
피격 이펙트: 착탄 지점에 0.2초 폭발 원 (radius 0→20, alpha 0.6→0)
```

### 4-4. Skill 이펙트 (시각 효과 상세)

#### 돌진 (charge) — 보병

```
1. 시작: 보병 주변 파란 빛 링 (반경 20→50px, 0.3초, alpha 0.8→0)
2. 이동 중: 뒤에 파란 잔상 3개 (alpha 0.3/0.2/0.1, 간격 0.05초)
3. 타격 시: 타격 유닛 주변 충격파 링 (반경 0→60px, 흰색, 0.2초)

색상: #4488ff → #88aaff 그라디언트
```

#### 포격 (barrage) — 전차

```
1. 충전: 전차 포신에서 주황 빛이 0.5초간 커짐 (radius 5→20)
2. 발사: 화면 흔들림 (camera.shake(200, 0.01))
3. 착탄: 반경 100px 내 주황 폭발
   - 중심 원: radius 0→40, #ffaa00, alpha 1→0, 0.3초
   - 파편: 8방향 작은 선 (길이 20px), #ffcc44, 0.2초 fadeOut
4. 지속 불꽃: 착탄 지점에 0.5초간 작은 불꽃 2~3개

색상: #ffaa00, #ff6600, #ffdd44
```

#### 폭격 (airstrike) — 공군

```
1. 조준: 타깃 영역에 빨간 조준선 X 표시 (0.5초 깜빡임)
2. 폭격기 그림자: 화면 상단에서 타깃 방향으로 그림자 이동 (0.3초)
3. 폭발 5회 (0.4초 간격):
   - 각 폭발: radius 0→30, #ff4400, alpha 0.8→0, 0.3초
   - 연기: 회색 원 위로 떠오름 (y-=30, alpha 0.5→0, 1초)
4. 타격 효과: 피격 유닛 빨간 플래시 × 5회

색상: #ff4400, #ff6600, #ffff00 (중심), #666666 (연기)
```

#### 힐 (heal) — 특수

```
1. 특수 유닛에서 초록 파동 발생 (반경 0→200, #44ff88, alpha 0.6→0, 0.5초)
2. 아군 유닛 각각:
   - 초록 하트/십자 파티클 3개가 위로 떠오름 (y-=20, 1초)
   - 유닛 주변 초록 링 0.3초 표시
   - HP 바 즉시 재드로우 (증가 애니메이션)
3. HP 바 증가 애니메이션:
   - 현재값 → 회복값 까지 0.5초 Tween (수치가 올라가는 효과)

색상: #44ff88, #88ffaa
```

### 4-5. Die (사망)

```
1. 피격 마지막 순간: 빨간 플래시 0.1초
2. 쓰러짐: rotation 0→90도 Tween, 0.4초, ease: 'Expo.In'
3. 페이드아웃: alpha 1→0, 0.5초
4. 잔해: 작은 회색 X 마크 0.5초 표시 후 사라짐
5. 파티클: 3~5개 작은 조각이 사방으로 튀어나감 (ObjectPool 사용)

총 지속: 0.8초
사망 후 오브젝트: setActive(false), setVisible(false)
```

---

## 5. UI 디자인 상세

### 5-1. 전투 HUD 레이아웃

```
┌────────────────────────────────────────────┐
│  ⏱ 09:45        🏴 2 : 1 ⚑               │  h=44px, HUD 상단 바
│  [공격 모드]     타이머    점령 점수        │
├──────────┬─────────────────────────────────┤
│          │                                 │
│ SWIPE    │                                 │
│  ZONE    │    Phaser Canvas (맵)           │
│          │                                 │
│  [오토]  │                    ┌──┬──┬──┬──┐│
│          │                    │①│②│③│④││  스킬 버튼
│          │                    │🗡│🔫│💣│❤││
│          │                    └──┴──┴──┴──┘│
└──────────┴─────────────────────────────────┘
  30%              70%
```

### 5-2. 스킬 버튼 (각 48×48px)

```
상태별 표현:
  대기중:    버튼 색상 + 스킬 아이콘 (불투명)
  쿨타임:    어두운 오버레이 + 남은 시간 숫자 (초)
            → 시계방향으로 채워지는 반투명 arc로 표현
  발동중:    테두리 흰색 pulse 효과

버튼 크기: 48×48px
간격: 8px
위치: 맵 우하단, x=600~752, y=500~548
```

### 5-3. 오토/수동 토글

```
크기: 64×28px 슬라이드 토글
오토 ON:  배경 #4488ff, 레이블 "AUTO"
수동 OFF: 배경 #444466, 레이블 "MANUAL"
위치: 스킬 버튼 아래 y=560
```

### 5-4. HP 바 애니메이션

```
변화 시:    현재값 → 새값으로 0.2초 Tween (smooth update)
색상 규칙:
  HP > 50%:  #44ff44 (초록)
  HP > 25%:  #ffff00 (노랑)
  HP ≤ 25%:  #ff4444 (빨강)
  임계값 도달 시 색상 전환 애니메이션 0.3초

바 크기: 28×4px (유닛 위)
위치:   유닛 y-22
```

### 5-5. 전투 결과 화면

```
레이아웃 (전체 화면 오버레이):

  배경: rgba(0,0,0,0.85) + 중앙 카드 400×500px

  ┌─────────────────────┐
  │                     │
  │   🏆 승리! / 💀 패배 │  72px 폰트, 색상 코딩
  │                     │
  │  ─────────────────  │
  │  생존 유닛   3 / 5   │
  │  전투 시간   4:32    │
  │  ─────────────────  │
  │  🪙 +200  ⭐ +100   │  자원 / 명성 획득
  │                     │
  │   [ 로비로 돌아가기 ] │
  └─────────────────────┘

등장 애니메이션:
  카드: scale 0.5→1.0, alpha 0→1, 0.4초, ease: Back.Out
  수치: 0부터 목표값까지 카운트업 (0.8초)
```

---

## 6. 로비 화면 디자인

### 6-1. 전체 레이아웃

```
배경: linear-gradient(180deg, #0a0a1a 0%, #1a1a2e 100%)

┌───────────────────────────────────┐
│  🪙 1,000  ⭐ 500  [병사 🎖]      │  h=52px, TopBar
├───────────────────────────────────┤
│  내 부대 (24)                     │  h=24px, 섹션 헤더
│ ┌──┐┌──┐┌──┐┌──┐                  │
│ │보││보││전││공│  ...             │  유닛 카드 그리드 (4열)
│ │T1││T2││T1││T3│                  │  각 64×80px
│ └──┘└──┘└──┘└──┘                  │
│  (스크롤 가능, 최대 24개)          │  flex-wrap
├───────────────────────────────────┤
│  덱 편성 (5/5)                    │  h=24px, 섹션 헤더
│ ┌──┐┌──┐┌──┐┌──┐┌──┐             │
│ │보││전││공││특││  │             │  덱 슬롯 5개 (64×80px)
│ │T2││T1││T3││T1││ + │             │  빈 슬롯: 점선 테두리
│ └──┘└──┘└──┘└──┘└──┘             │
├───────────────────────────────────┤
│       [ ⚔ 전투 시작 ]             │  h=56px, CTA 버튼
└───────────────────────────────────┘
```

### 6-2. 유닛 카드 (64×80px)

```
┌────────────────┐
│    [스프라이트] │  44×44px (타입별 색상 블록)
│                │
│  보병   T2     │  이름 10px / 티어 배지
│ ⚔10 🛡14 ❤100 │  주요 스탯 3종 (8px)
└────────────────┘

드래그 상태: opacity 0.6, scale 1.05, shadow
덱 안 유닛:  파란 외곽선 2px, 우상단 ✓ 배지
```

### 6-3. 드래그&드롭 시각 피드백

```
드래그 시작:   원본 카드 opacity 0.3
드래그 중:     유령 카드 (cursor 위치 따라감), 그림자 효과
드롭 가능 슬롯: 파란 빛 pulse 효과 (border-color #4488ff)
드롭 불가 슬롯: 빨간 빛 (#ff4444)
드롭 성공:     카드 scale 1.2→1.0 bounce (0.2초)
```

---

## 7. 파티클 & 공통 이펙트 규칙

### 7-1. ObjectPool 기반 파티클

```
모든 파티클은 ObjectPool에서 가져와 재사용.
Pool 크기:
  투사체:  40개
  파티클:  60개 (작은 원/사각형)
  텍스트:  20개 (데미지 숫자 팝업)
```

### 7-2. 데미지 숫자 팝업

```
위치:    피격 유닛 y-30
크기:    14px, bold
색상:    일반 피해=#ffdd00, 크리티컬=#ff4444, 회복=#44ff88
애니메이션: y-=30 + alpha 1→0, 0.8초
재사용:  ObjectPool에서 BitmapText 오브젝트 꺼내 쓰기
```

### 7-3. 카메라 이펙트

```
화면 흔들림 사용 시나리오:
  전차 포격:  camera.shake(200ms, 0.01 intensity)
  폭격 착탄: camera.shake(100ms, 0.005 intensity)
  플레이어 전멸: camera.flash(300ms, 255, 0, 0) → 빨간 플래시
  승리:       camera.flash(500ms, 255, 255, 100) → 황금 플래시
```

---

## 8. 에셋 파일 구조

```
public/assets/
├── sprites/
│   ├── infantry.png        (64×256, 4행 스프라이트 시트)
│   ├── tank.png
│   ├── air.png
│   ├── special.png
│   ├── enemy_infantry.png
│   ├── enemy_tank.png
│   ├── enemy_air.png
│   └── enemy_special.png
├── tiles/
│   ├── grass.png           (40×40 타일)
│   ├── road.png
│   └── mountain.png
├── ui/
│   ├── skill_charge.png    (48×48 스킬 아이콘)
│   ├── skill_barrage.png
│   ├── skill_airstrike.png
│   └── skill_heal.png
└── icons/
    ├── icon-192.png        (PWA 아이콘)
    └── icon-512.png
```

**Phase 0 Placeholder 생성**:
- 모든 이미지는 Phaser `graphics.generateTexture()` 로 런타임 생성
- 파일 없어도 동작 가능, 추후 실제 PNG로 교체만 하면 됨

---

## 11. 스프라이트 PNG 생성 전략

### Phase 0 — Placeholder (현재, 코드로 자동 생성)

**담당: `asset-agent`** — PNG 파일 없이 Phaser API로 런타임 생성

```typescript
// LoadingScene.ts preload() 내에서 실행
// 예: 보병 — 파란 원 + 흰 외곽선
scene.make.graphics({ add: false })
  .fillStyle(0x4488ff).fillCircle(16, 16, 14)
  .lineStyle(2, 0xffffff).strokeCircle(16, 16, 14)
  .generateTexture('unit_infantry', 32, 32).destroy();

// 예: 전차 — 주황 사각형 + 포신
scene.make.graphics({ add: false })
  .fillStyle(0xffaa00).fillRoundedRect(2, 6, 28, 20, 3)
  .fillStyle(0xcc8800).fillRect(12, 2, 8, 24)
  .generateTexture('unit_tank', 32, 32).destroy();
```

생성 대상 텍스처 키:
```
유닛:   unit_infantry / unit_tank / unit_air / unit_special
적군:   enemy_infantry / enemy_tank / enemy_air / enemy_special
지형:   tile_grass / tile_road / tile_mountain
거점:   cp_neutral / cp_player / cp_enemy
스킬:   skill_charge / skill_barrage / skill_airstrike / skill_heal
투사체: projectile_bullet / projectile_missile
```

---

### Phase 1 — 실제 스프라이트 제작 옵션

#### 옵션 A. AI 이미지 생성 (권장 — 가장 빠름)

| 도구 | 특징 | 비용 |
|---|---|---|
| **Midjourney** | 고품질 군사/픽셀 스타일, 스프라이트 시트 가능 | $10/월~ |
| **DALL-E 3** | ChatGPT에서 바로 사용, 단순 유닛에 적합 | ChatGPT Plus |
| **Stable Diffusion** | 무료, ControlNet 포즈 제어, GPU 필요 | 무료 |

Midjourney 프롬프트 예시:
```
top-down pixel art sprite sheet, military infantry soldier,
4 frames idle animation, transparent background,
64x64 pixels per frame, 256x64 total canvas,
green uniform, flat simple design --ar 4:1 --style raw
```

#### 옵션 B. 무료 오픈소스 에셋 (즉시 사용 가능)

| 사이트 | 추천 팩 | 라이선스 |
|---|---|---|
| **Kenney.nl** | topdown-tanks-redux, topdown-shooter | CC0 완전 무료 |
| **OpenGameArt.org** | "top down unit", "strategy game sprites" | CC0/CC-BY |
| **itch.io** | "top-down military sprites" | 대부분 무료 |

#### 옵션 C. 직접 픽셀 아트 제작

| 도구 | 비용 | 특징 |
|---|---|---|
| **Aseprite** | $20 (1회) | 스프라이트 전용, 시트 내보내기 탁월 |
| **Libresprite** | 무료 | Aseprite 오픈소스 포크 |
| **Piskel** | 무료 | 웹 브라우저에서 즉시 사용 |

Aseprite 내보내기 설정:
```
File → Export Sprite Sheet
  Layout: Horizontal strip
  Columns: idle=4, move=6, attack=4, die=4
  Frame size: 64×64
  Output: infantry.png (512×256)
```

#### 옵션 D. Node.js Canvas 스크립트 (asset-agent 확장)

```bash
npm install canvas
node scripts/generate-sprites.mjs
# → public/assets/sprites/*.png 일괄 생성
```

```javascript
// scripts/generate-sprites.mjs
import { createCanvas } from 'canvas';
import fs from 'fs';

function makeSheet(color, outPath) {
  const c = createCanvas(512, 256);
  const ctx = c.getContext('2d');
  // idle 4프레임 (row 0)
  for (let i = 0; i < 4; i++) {
    ctx.fillStyle = color;
    ctx.beginPath();
    ctx.arc(32 + i * 64, 32, 22, 0, Math.PI * 2);
    ctx.fill();
  }
  fs.writeFileSync(outPath, c.toBuffer('image/png'));
}

makeSheet('#4488ff', 'public/assets/sprites/infantry.png');
makeSheet('#ffaa00', 'public/assets/sprites/tank.png');
makeSheet('#44ffcc', 'public/assets/sprites/air.png');
makeSheet('#ff44aa', 'public/assets/sprites/special.png');
```

---

### Placeholder → 실제 스프라이트 교체 방법

```
Step 1. PNG를 public/assets/sprites/ 에 배치 (파일명 규격 준수)

Step 2. LoadingScene.ts: generateTexture() → load.spritesheet() 교체
        this.load.spritesheet('unit_infantry',
          `${import.meta.env.BASE_URL}assets/sprites/infantry.png`,
          { frameWidth: 64, frameHeight: 64 }
        );

Step 3. Unit.ts: 애니메이션 등록 활성화
        this.anims.create({
          key: 'infantry_idle',
          frames: this.anims.generateFrameNumbers('unit_infantry', { start: 0, end: 3 }),
          frameRate: 8, repeat: -1,
        });

Step 4. hitbox 크기 조정 (32×32 → 64×64)
```

---

### 권장 진행 순서

```
Phase 0  →  asset-agent: generateTexture()로 전부 처리 (PNG 불필요)
                ↓ 게임플레이 로직 완성 & 검증
Phase 1  →  Kenney.nl 무료팩 즉시 적용 (1~2일)
                ↓ 비주얼 방향 확정
Phase 2  →  Midjourney 커스텀 스프라이트 or 픽셀아트 아티스트 의뢰
```

> **Phase 0에서는 PNG 파일이 전혀 없어도 됩니다.**
> `asset-agent`가 코드로 모든 placeholder를 생성합니다.

---

## 9. Phaser 애니메이션 키 네이밍 규칙

```typescript
// 애니메이션 키 형식: "{unitType}_{state}"
// 예:
'infantry_idle'    // 보병 대기
'infantry_move'    // 보병 이동
'infantry_attack'  // 보병 공격
'infantry_die'     // 보병 사망
'tank_idle'
'tank_move'
// ...

// Phaser.Scene.anims.create() 에서 위 키로 등록
// Unit.ts 에서 this.play('{type}_{state}') 호출
```

---

## 10. 성능 주의사항

```
애니메이션 관련 성능 규칙:
  ✅ Phaser.Tweens.add() — Tween Manager가 관리, 성능 안전
  ✅ Phaser.GameObjects.Particles — Emitter 재사용
  ✅ BitmapText — Text보다 빠른 숫자 렌더링
  ❌ 매 프레임 new Tween 생성 금지
  ❌ 매 프레임 Graphics.clear() + 재드로우 금지
     → 값 변경 시에만 호출

Graphics 재드로우 최적화:
  HP 바: 이전 hp와 현재 hp 비교, 변경된 경우에만 redrawHpBar()
  거점 링: captureProgress 변경 시에만 재드로우
```
